<form method="post" action="{{ path('crud_support_delete', {'id': crud_support.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ crud_support.id) }}">
    <button class="btn">Delete</button>
</form>
